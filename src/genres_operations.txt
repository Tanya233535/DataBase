INSERT INTO genres (genre) VALUES ('Fantasy');
DELETE FROM genres WHERE genre = 'Fantasy';
DELETE FROM genres WHERE id = 1;
Операция не выполнена. В базе данных возникает ошибка из-за наличия внешнего ключа в таблице books, который ссылается на этот жанр.

Причина: В базе данных существует ограничение внешнего ключа (FOREIGN KEY), которое не позволяет удалить запись из таблицы genres, если на нее ссылается какая-либо запись в таблице books. Это ограничение обеспечивает целостность данных и предотвращает появление "висящих" ссылок.